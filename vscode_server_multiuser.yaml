AWSTemplateFormatVersion: '2010-09-09'
Description: 'Multi-User VSCode Server on ECS - 10 containers for 10 users with individual workspaces'

Metadata:
  "AWS::CloudFormation::Interface":
    ParameterGroups:
      - Label:
          default: "Network Configuration"
        Parameters:
         - AvailabilityZoneA
         - AvailabilityZoneB
         - VPCCIDRBlock
         - PublicSubnetABlock
         - PublicSubnetBBlock
         - PrivateSubnetABlock
         - PrivateSubnetBBlock
      - Label:
          default: "VSCode Configuration"
        Parameters:
         - DefaultPassword
         - TaskCpu
         - TaskMemory
         - UserCount

Parameters:
  CloudFrontPrefixListId:
    Type: String
    Description: CloudFront origin-facing managed prefix list ID
    
  AvailabilityZoneA:
    Description: "Choose AZ1 for your VPC"
    Type: AWS::EC2::AvailabilityZone::Name
    Default: ap-northeast-2a
    
  AvailabilityZoneB:
    Description: "Choose AZ2 for your VPC"
    Type: AWS::EC2::AvailabilityZone::Name
    Default: ap-northeast-2b

  VPCCIDRBlock:
    Type: String
    Default: "10.254.0.0/16"
    Description: "CIDR range for the VPC"

  PublicSubnetABlock:
    Type: String
    Default: "10.254.11.0/24"
    Description: "CIDR for public subnet A"

  PublicSubnetBBlock:
    Type: String
    Default: "10.254.12.0/24"
    Description: "CIDR for public subnet B"

  PrivateSubnetABlock:
    Type: String
    Default: "10.254.21.0/24"
    Description: "CIDR for private subnet A"

  PrivateSubnetBBlock:
    Type: String
    Default: "10.254.22.0/24"
    Description: "CIDR for private subnet B"

  DefaultPassword:
    Type: String
    NoEcho: true
    MinLength: 8
    Description: "Default password for all VSCode instances (users can change later)"

  TaskCpu:
    Type: String
    Default: "1024"
    Description: "CPU units per task (1024 = 1 vCPU)"
    AllowedValues:
      - "512"
      - "1024"
      - "2048"

  TaskMemory:
    Type: String
    Default: "2048"
    Description: "Memory per task (MB)"
    AllowedValues:
      - "1024"
      - "2048"
      - "4096"

  UserCount:
    Type: Number
    Default: 10
    MinValue: 1
    MaxValue: 20
    Description: "Number of VSCode containers to create"

Resources:
  # VPC Configuration (same as before)
  VPC:
    Type: AWS::EC2::VPC
    Properties:
      CidrBlock: !Ref VPCCIDRBlock
      EnableDnsSupport: true
      EnableDnsHostnames: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-VPC'

  InternetGateway:
    Type: AWS::EC2::InternetGateway
    Properties:
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-IGW'

  AttachGateway:
    Type: AWS::EC2::VPCGatewayAttachment
    Properties:
      VpcId: !Ref VPC
      InternetGatewayId: !Ref InternetGateway

  NatGatewayEIP:
    DependsOn: AttachGateway
    Type: AWS::EC2::EIP
    Properties:
      Domain: vpc
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-NATGW-EIP'

  NatGateway:
    Type: AWS::EC2::NatGateway
    Properties:
      AllocationId: !GetAtt NatGatewayEIP.AllocationId
      SubnetId: !Ref PublicSubnetA
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-NATGW'

  # Public Subnets
  PublicSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref PublicSubnetABlock
      AvailabilityZone: !Ref AvailabilityZoneA
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PublicSubnetA'

  PublicSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref PublicSubnetBBlock
      AvailabilityZone: !Ref AvailabilityZoneB
      MapPublicIpOnLaunch: true
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PublicSubnetB'

  PublicRouteTable:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PublicRT'

  PublicRoute:
    DependsOn: AttachGateway
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref PublicRouteTable
      DestinationCidrBlock: 0.0.0.0/0
      GatewayId: !Ref InternetGateway

  PublicSubnetARouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PublicSubnetA
      RouteTableId: !Ref PublicRouteTable

  PublicSubnetBRouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PublicSubnetB
      RouteTableId: !Ref PublicRouteTable

  # Private Subnets
  PrivateSubnetA:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref PrivateSubnetABlock
      AvailabilityZone: !Ref AvailabilityZoneA
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PrivateSubnetA'

  PrivateSubnetB:
    Type: AWS::EC2::Subnet
    Properties:
      VpcId: !Ref VPC
      CidrBlock: !Ref PrivateSubnetBBlock
      AvailabilityZone: !Ref AvailabilityZoneB
      MapPublicIpOnLaunch: false
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PrivateSubnetB'

  PrivateRouteTableA:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PrivateRT-A'

  PrivateRouteTableB:
    Type: AWS::EC2::RouteTable
    Properties:
      VpcId: !Ref VPC
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-PrivateRT-B'

  PrivateRouteA:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref PrivateRouteTableA
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway

  PrivateRouteB:
    Type: AWS::EC2::Route
    Properties:
      RouteTableId: !Ref PrivateRouteTableB
      DestinationCidrBlock: 0.0.0.0/0
      NatGatewayId: !Ref NatGateway

  PrivateSubnetARouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PrivateSubnetA
      RouteTableId: !Ref PrivateRouteTableA

  PrivateSubnetBRouteTableAssociation:
    Type: AWS::EC2::SubnetRouteTableAssociation
    Properties:
      SubnetId: !Ref PrivateSubnetB
      RouteTableId: !Ref PrivateRouteTableB

  # EFS for shared workspace storage
  EFSFileSystem:
    Type: AWS::EFS::FileSystem
    Properties:
      CreationToken: !Sub '${AWS::StackName}-efs'
      PerformanceMode: generalPurpose
      ThroughputMode: provisioned
      ProvisionedThroughputInMibps: 100
      FileSystemTags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-EFS'

  EFSMountTargetA:
    Type: AWS::EFS::MountTarget
    Properties:
      FileSystemId: !Ref EFSFileSystem
      SubnetId: !Ref PrivateSubnetA
      SecurityGroups:
        - !Ref EFSSecurityGroup

  EFSMountTargetB:
    Type: AWS::EFS::MountTarget
    Properties:
      FileSystemId: !Ref EFSFileSystem
      SubnetId: !Ref PrivateSubnetB
      SecurityGroups:
        - !Ref EFSSecurityGroup

  # Security Groups
  PublicALBSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: "Security group for Internet-facing ALB"
      GroupName: !Sub '${AWS::StackName}-Public-ALB-SG'
      VpcId: !Ref VPC
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          SourcePrefixListId: !Ref CloudFrontPrefixListId
          Description: "HTTP from CloudFront origin-facing only"
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-Public-ALB-SG'

  ECSSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: "Security group for ECS VSCode tasks"
      GroupName: !Sub '${AWS::StackName}-ECS-SG'
      VpcId: !Ref VPC
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 8888
          ToPort: 8888
          SourceSecurityGroupId: !Ref PublicALBSecurityGroup
          Description: "VSCode port from Public ALB only"
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-ECS-SG'

  EFSSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: "Security group for EFS"
      GroupName: !Sub '${AWS::StackName}-EFS-SG'
      VpcId: !Ref VPC
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 2049
          ToPort: 2049
          SourceSecurityGroupId: !Ref ECSSecurityGroup
          Description: "NFS from ECS tasks"
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-EFS-SG'

  # ECS Cluster
  ECSCluster:
    Type: AWS::ECS::Cluster
    Properties:
      ClusterName: !Sub '${AWS::StackName}-cluster'
      CapacityProviders:
        - FARGATE
      DefaultCapacityProviderStrategy:
        - CapacityProvider: FARGATE
          Weight: 1
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-ECS-Cluster'

  # CloudWatch Log Group
  VSCodeLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub '/ecs/${AWS::StackName}-vscode'
      RetentionInDays: 7

  # IAM Roles
  ECSExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${AWS::StackName}-ECS-Execution-Role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AmazonECSTaskExecutionRolePolicy
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-ECS-Execution-Role'

  ECSTaskRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub '${AWS::StackName}-ECS-Task-Role'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: ecs-tasks.amazonaws.com
            Action: sts:AssumeRole
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-ECS-Task-Role'

  # Multi-User Task Definition and Services
  VSCodeTaskDefinition:
    Type: AWS::ECS::TaskDefinition
    Properties:
      Family: !Sub '${AWS::StackName}-vscode'
      NetworkMode: awsvpc
      RequiresCompatibilities:
        - FARGATE
      Cpu: !Ref TaskCpu
      Memory: !Ref TaskMemory
      ExecutionRoleArn: !Ref ECSExecutionRole
      TaskRoleArn: !Ref ECSTaskRole
      Volumes:
        - Name: efs-volume
          EFSVolumeConfiguration:
            FileSystemId: !Ref EFSFileSystem
            RootDirectory: "/"
            TransitEncryption: ENABLED
      ContainerDefinitions:
        - Name: vscode-server
          Image: codercom/code-server:4.106.3
          Essential: true
          PortMappings:
            - ContainerPort: 8888
              Protocol: tcp
          Environment:
            - Name: PASSWORD
              Value: !Ref DefaultPassword
          Command:
            - "--bind-addr"
            - "0.0.0.0:8888"
            - "--auth"
            - "password"
            - "--config-dir"
            - "/config"
            - "--user-data-dir"
            - "/userdata"
            - "--extensions-dir"
            - "/extensions"
            - "/workspace"
          MountPoints:
            - SourceVolume: efs-volume
              ContainerPath: /workspace
              ReadOnly: false
            - SourceVolume: efs-volume
              ContainerPath: /config
              ReadOnly: false
            - SourceVolume: efs-volume
              ContainerPath: /userdata
              ReadOnly: false
            - SourceVolume: efs-volume
              ContainerPath: /extensions
              ReadOnly: false
          LogConfiguration:
            LogDriver: awslogs
            Options:
              awslogs-group: !Ref VSCodeLogGroup
              awslogs-region: !Ref AWS::Region
              awslogs-stream-prefix: vscode
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-VSCode-Task'

  # Individual ECS Services for each user
  User1Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user1-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User1TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User1-Service'

  User2Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user2-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User2TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User2-Service'

  User3Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user3-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User3TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User3-Service'

  User4Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user4-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User4TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User4-Service'

  User5Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user5-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User5TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User5-Service'

  User6Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user6-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User6TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User6-Service'

  User7Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user7-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User7TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User7-Service'

  User8Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user8-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User8TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User8-Service'

  User9Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user9-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User9TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User9-Service'

  User10Service:
    Type: AWS::ECS::Service
    DependsOn: PublicALBListener
    Properties:
      ServiceName: !Sub '${AWS::StackName}-user10-service'
      Cluster: !Ref ECSCluster
      TaskDefinition: !Ref VSCodeTaskDefinition
      LaunchType: FARGATE
      DesiredCount: 1
      NetworkConfiguration:
        AwsvpcConfiguration:
          SecurityGroups:
            - !Ref ECSSecurityGroup
          Subnets:
            - !Ref PrivateSubnetA
            - !Ref PrivateSubnetB
          AssignPublicIp: DISABLED
      LoadBalancers:
        - ContainerName: vscode-server
          ContainerPort: 8888
          TargetGroupArn: !Ref User10TargetGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User10-Service'

  # Application Load Balancer with Path-based Routing
  PublicALB:
    Type: AWS::ElasticLoadBalancingV2::LoadBalancer
    Properties:
      Name: !Sub "${AWS::StackName}-Public-ALB"
      Type: application
      Scheme: internet-facing
      IpAddressType: ipv4
      LoadBalancerAttributes:
        - Key: idle_timeout.timeout_seconds
          Value: 3600
      Subnets:
        - !Ref PublicSubnetA
        - !Ref PublicSubnetB
      SecurityGroups:
        - !Ref PublicALBSecurityGroup
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-Public-ALB'

  # Target Groups for each user (1-10)
  User1TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User1-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User1-TG'

  User2TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User2-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User2-TG'

  User3TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User3-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User3-TG'

  User4TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User4-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User4-TG'

  User5TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User5-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User5-TG'

  User6TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User6-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User6-TG'

  User7TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User7-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User7-TG'

  User8TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User8-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User8-TG'

  User9TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User9-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User9-TG'

  User10TargetGroup:
    Type: AWS::ElasticLoadBalancingV2::TargetGroup
    Properties:
      Name: !Sub "${AWS::StackName}-User10-TG"
      VpcId: !Ref VPC
      Protocol: HTTP
      Port: 8888
      TargetType: ip
      HealthCheckEnabled: true
      HealthCheckProtocol: HTTP
      HealthCheckPath: "/"
      HealthCheckIntervalSeconds: 30
      HealthCheckTimeoutSeconds: 5
      HealthyThresholdCount: 2
      UnhealthyThresholdCount: 3
      Matcher:
        HttpCode: 200,302
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-User10-TG'

  PublicALBListener:
    Type: AWS::ElasticLoadBalancingV2::Listener
    Properties:
      LoadBalancerArn: !Ref PublicALB
      Protocol: HTTP
      Port: 80
      DefaultActions:
        - Type: fixed-response
          FixedResponseConfig:
            StatusCode: 403
            ContentType: text/html
            MessageBody: |
              <html>
              <body>
              <h1>VSCode Server - User Selection</h1>
              <p>Please select your user workspace:</p>
              <ul>
                <li><a href="/user1">User 1 Workspace</a></li>
                <li><a href="/user2">User 2 Workspace</a></li>
                <li><a href="/user3">User 3 Workspace</a></li>
                <li><a href="/user4">User 4 Workspace</a></li>
                <li><a href="/user5">User 5 Workspace</a></li>
                <li><a href="/user6">User 6 Workspace</a></li>
                <li><a href="/user7">User 7 Workspace</a></li>
                <li><a href="/user8">User 8 Workspace</a></li>
                <li><a href="/user9">User 9 Workspace</a></li>
                <li><a href="/user10">User 10 Workspace</a></li>
              </ul>
              </body>
              </html>

  # Path-based routing rules for each user
  User1ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 1
      Conditions:
        - Field: path-pattern
          Values:
            - "/user1*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User1TargetGroup

  User2ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 2
      Conditions:
        - Field: path-pattern
          Values:
            - "/user2*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User2TargetGroup

  User3ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 3
      Conditions:
        - Field: path-pattern
          Values:
            - "/user3*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User3TargetGroup

  User4ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 4
      Conditions:
        - Field: path-pattern
          Values:
            - "/user4*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User4TargetGroup

  User5ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 5
      Conditions:
        - Field: path-pattern
          Values:
            - "/user5*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User5TargetGroup

  User6ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 6
      Conditions:
        - Field: path-pattern
          Values:
            - "/user6*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User6TargetGroup

  User7ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 7
      Conditions:
        - Field: path-pattern
          Values:
            - "/user7*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User7TargetGroup

  User8ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 8
      Conditions:
        - Field: path-pattern
          Values:
            - "/user8*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User8TargetGroup

  User9ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 9
      Conditions:
        - Field: path-pattern
          Values:
            - "/user9*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User9TargetGroup

  User10ListenerRule:
    Type: AWS::ElasticLoadBalancingV2::ListenerRule
    Properties:
      ListenerArn: !Ref PublicALBListener
      Priority: 10
      Conditions:
        - Field: path-pattern
          Values:
            - "/user10*"
        - Field: http-header
          HttpHeaderConfig:
            HttpHeaderName: X-Custom-Secret
            Values:
              - !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
      Actions:
        - Type: forward
          TargetGroupArn: !Ref User10TargetGroup

  # CloudFront Distribution
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        Comment: !Sub "Multi-User VSCode Server ECS distribution for ${AWS::StackName}"
        Origins:
          - Id: PublicALBOrigin
            DomainName: !GetAtt PublicALB.DNSName
            CustomOriginConfig:
              HTTPPort: 80
              OriginProtocolPolicy: http-only
              OriginReadTimeout: 60
              OriginKeepaliveTimeout: 5
            OriginCustomHeaders:
              - HeaderName: X-Custom-Secret
                HeaderValue: !Sub '${AWS::StackName}-secret-${AWS::AccountId}'
        DefaultCacheBehavior:
          TargetOriginId: PublicALBOrigin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods:
            - GET
            - HEAD
            - OPTIONS
            - PUT
            - POST
            - PATCH
            - DELETE
          CachedMethods:
            - GET
            - HEAD
          Compress: true
          ForwardedValues:
            QueryString: true
            Cookies:
              Forward: all
            Headers:
              - '*'
          MinTTL: 0
          DefaultTTL: 0
          MaxTTL: 0
        PriceClass: PriceClass_All
        ViewerCertificate:
          CloudFrontDefaultCertificate: true
        Restrictions:
          GeoRestriction:
            RestrictionType: none
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-CloudFront'

Outputs:
  VPCId:
    Description: VPC ID
    Value: !Ref VPC
    Export:
      Name: !Sub "${AWS::StackName}-VPC-ID"

  CloudFrontURL:
    Description: CloudFront Distribution URL (Use this to access VSCode)
    Value: !Sub "https://${CloudFrontDistribution.DomainName}"
    Export:
      Name: !Sub "${AWS::StackName}-CloudFront-URL"

  ECSClusterName:
    Description: ECS Cluster Name
    Value: !Ref ECSCluster
    Export:
      Name: !Sub "${AWS::StackName}-ECS-Cluster"

  EFSFileSystemId:
    Description: EFS File System ID for shared workspace
    Value: !Ref EFSFileSystem
    Export:
      Name: !Sub "${AWS::StackName}-EFS-ID"

  DefaultPassword:
    Description: Default password for all VSCode instances
    Value: "Users can change password by accessing VSCode settings"

  UserInstructions:
    Description: Instructions for users
    Value: !Sub |
      Individual User URLs:
      1. User 1: https://${CloudFrontDistribution.DomainName}/user1
      2. User 2: https://${CloudFrontDistribution.DomainName}/user2
      3. User 3: https://${CloudFrontDistribution.DomainName}/user3
      4. User 4: https://${CloudFrontDistribution.DomainName}/user4
      5. User 5: https://${CloudFrontDistribution.DomainName}/user5
      6. User 6: https://${CloudFrontDistribution.DomainName}/user6
      7. User 7: https://${CloudFrontDistribution.DomainName}/user7
      8. User 8: https://${CloudFrontDistribution.DomainName}/user8
      9. User 9: https://${CloudFrontDistribution.DomainName}/user9
      10. User 10: https://${CloudFrontDistribution.DomainName}/user10
      
      Main Page: https://${CloudFrontDistribution.DomainName} (shows user selection)
      Default Password: Use the password provided during deployment
      
      Persistent Storage:
      - Workspace Files: /workspace (your code and projects)
      - VSCode Settings: /config (themes, preferences, keybindings)
      - User Data: /userdata (workspace settings, recent files)
      - Extensions: /extensions (installed extensions)
      
      Note: All settings, extensions, and files persist across container restarts
      Each user has dedicated container with persistent configuration

  Architecture:
    Description: Path-based Multi-User Architecture with Persistent Storage
    Value: "CloudFront -> ALB (Path-based Routing) -> 10x Dedicated ECS Fargate Containers -> Shared EFS Storage (Workspace + Config + Extensions)"
